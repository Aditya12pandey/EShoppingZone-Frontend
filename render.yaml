services:
  - type: web
    name: eshoppingzone-frontend
    runtime: static
    buildCommand: npm install && npm run build -- --configuration production
    staticPublishPath: ./dist/eshoppingzone-frontend/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
